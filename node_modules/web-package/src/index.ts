import gulp = require("gulp");
import Orchestrator = require("orchestrator");

gulp.task("build", (done: Orchestrator.AddMethodCallback) => {
    console.log("This task do nothing, stupid task...");
    done();
});